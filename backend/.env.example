# ⚠️ SECURITY WARNING ⚠️
# This is an EXAMPLE file. DO NOT commit your actual .env file to Git!
# Copy this file to .env and replace ALL values with your actual credentials.

# Database Configuration
# Get your PostgreSQL connection string from your database provider (Neon, Supabase, etc.)
DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# JWT Authentication
# Generate a secure random string (minimum 32 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your_secure_random_jwt_secret_min_32_characters_CHANGE_THIS

# JWT Token Expiration
JWT_EXPIRES_IN=1h

# Blockchain RPC URL
# Base Sepolia testnet RPC endpoint
RPC_URL=https://sepolia.base.org

# Smart Contract Addresses (Base Sepolia)
# Replace with your deployed contract addresses
CONTRACT_WALLET_REGISTRY=0xYourWalletRegistryContractAddress
CONTRACT_CERT_REGISTRY=0xYourCertificateRegistryContractAddress

# Server Configuration
PORT=3000
NODE_ENV=development

# ═══════════════════════════════════════════════════════════════
# SETUP INSTRUCTIONS:
# ═══════════════════════════════════════════════════════════════
# 1. Copy this file: cp .env.example .env
# 2. Replace ALL placeholder values with your actual credentials
# 3. NEVER commit the .env file to Git (it's in .gitignore)
# 4. Keep your credentials secure and private
# ═══════════════════════════════════════════════════════════════
